name: tet

on:
  push:
  workflow_call:
  workflow_dispatch:
  schedule:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: test the code
        run: echo "Code test using sonar/pytest"
